# Research API Specification

## GET /metadata/{pdb_id}

Returns protein metadata placeholder.

## GET /docking/config/{pdb_id}

Returns docking box configuration.

## GET /experiments

Returns experiment run list.

## GET /experiments/{experiment_id}

Returns one experiment run record.
