# Backend

FastAPI backend for BioDockLab.

Planned responsibilities:

- Serve protein metadata
- Serve ligand metadata
- Serve docking result JSON
- Convert experiment outputs into frontend-friendly data
- Later: run controlled docking workflow scripts
